#!/usr/bin/env bash

#cd "$(dirname "$0")/../ruby/00_config"
#bundle exec ruby examples/example.rb

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
cd "$SCRIPT_DIR/../../ruby/03_prompt_builder"
bundle exec ruby examples/example.rb